spring:
  application:
    name: gateway #模块名称
  cloud:
    nacos:
      server-addr: 192.168.80.130
      config:
        file-extension: yml
        shared-configs:
          - data-id: share-log.yml
  profiles:
    active: dev
